// The software indexed baker for tests: now the package's own (src/soft.ts, softBake),
// re-exported here so the tests that import it from this fixture keep working.
export { softBake } from "../src/soft.ts";
