{
  "name": "@keel-engine/road",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Roads as data: sampled paths (splines, locate, pointAt), a road graph whose widths are rules, and a sparse per-chunk road field -- what a ground shader, a tyre and an AI driver all read. Deterministic dmath, no GL.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@keel-engine/core": "workspace:*",
    "@keel-engine/runtime": "workspace:*"
  }
}
