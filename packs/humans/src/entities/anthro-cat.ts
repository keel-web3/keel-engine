// A cat on two legs: pointed or tufted ears, a long tail, a short snout.
import { anthro } from "../species.ts";

export default anthro("cat", "Cat (anthro)", ["ears", "earSize", "coat", "tail", "snout"]);
