// A dog on two legs: floppy or pointed ears, a long tail, a long snout.
import { anthro } from "../species.ts";

export default anthro("dog", "Dog (anthro)", ["ears", "earSize", "coat", "tail", "snout"]);
